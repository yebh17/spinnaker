pipeline {
    agent any
    stages { 
        stage('Build') {
            steps {
                sh 'docker build -t python-app .' 
            }
        }

        stage('check'){
            steps {
                sh 'docker images'
            }
        }
    }

    post {
        always {
            cleanWs()
        }
    }
}